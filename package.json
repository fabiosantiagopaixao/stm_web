{
  "name": "stm_web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "gh-pages -d dist",
    "deploy:es": "gh-pages -d dist -b es",
    "deploy:ptBR": "gh-pages -d dist -b ptBr",
    "deploy:en": "gh-pages -d dist -b en",
    "gh:delete": "git branch -D gh-pages || true && git push origin --delete gh-pages"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
